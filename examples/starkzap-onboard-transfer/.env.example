# Test signer (generate: PRIVATE_KEY=0x$(openssl rand -hex 32))
PRIVATE_KEY=0x...

# Recipient for STRK transfer (or pass --recipient)
RECIPIENT_ADDRESS=0x...

# For sponsored (gasless) mode — get from https://portal.avnu.fi/
# AVNU_PAYMASTER_API_KEY=your_key

# Optional: custom RPC (default: PublicNode Sepolia)
# STARKNET_RPC_URL=https://starknet-sepolia-rpc.publicnode.com
