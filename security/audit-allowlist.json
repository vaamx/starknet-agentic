{
  "advisories": [
    {
      "id": 1113371,
      "package": "minimatch",
      "reason": "ReDoS in minimatch <10.2.1 — only reachable via eslint (devDependency, build-time only). Root override pins 10.2.2 but transitive eslint-plugin-import still resolves old range. Not exploitable at runtime.",
      "expiresOn": "2026-06-01"
    }
  ]
}
