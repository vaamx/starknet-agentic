import type { Standard } from "@/data/types";
interface StandardCardProps {
    standard: Standard;
}
export declare function StandardCard({ standard }: StandardCardProps): import("react").JSX.Element;
export {};
