import { resolveRpcSpecVersion as resolveRpcSpecVersionShared } from '../../../scripts/rpc-spec-version.mjs';
export function resolveRpcSpecVersion(value) {
    return resolveRpcSpecVersionShared(value);
}
